"""CLI entry point for chat-history-parser.

This module serves as the entry point when running the package as a module:
    python -m chat_history_parser
"""

from chat_history_parser.cli import main

if __name__ == "__main__":
    main()
